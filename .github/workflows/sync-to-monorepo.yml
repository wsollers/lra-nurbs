# =============================================================
# sync-to-monorepo.yml — lra-nurbs
# Syncs repo root content back to Learning-Real-Analysis/nurbs_dde/
# whenever changes are pushed to main.
#
# Setup:
#   Add a secret named SYNC_PAT in this repo's settings
#   (Settings → Secrets and variables → Actions)
#   with a GitHub PAT that has repo scope.
# =============================================================

name: Sync lra-nurbs to monorepo

on:
  push:
    branches: [main]
    paths-ignore:
      - '.github/**'
      - 'README.md'

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout lra-nurbs
        uses: actions/checkout@v4
        with:
          path: source

      - name: Checkout monorepo
        uses: actions/checkout@v4
        with:
          repository: wsollers/Learning-Real-Analysis
          token: ${{ secrets.SYNC_PAT }}
          path: monorepo

      - name: Sync repo root into monorepo/nurbs_dde/
        run: |
          # Sync everything except .github/ and README.md
          rsync -av --delete \
            --exclude='.github/' \
            --exclude='README.md' \
            source/ monorepo/nurbs_dde/

      - name: Commit and push if changed
        run: |
          cd monorepo
          git config user.name  "lra-sync-bot"
          git config user.email "sync-bot@noreply.github.com"
          git add nurbs_dde/
          git diff --cached --quiet && echo "No changes to sync" && exit 0
          git commit -m "chore: sync nurbs_dde/ from lra-nurbs"
          git push
